{
  "name": "vv-cockpit",
  "version": "0.1.0",
  "private": true,
  "description": "VV-Cockpit Docker PWA: port of helper, duty planning and sales application",
  "type": "commonjs",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/server.js && node --check src/runtime.js && node --check src/sheets.js && node --test tests/*.test.js"
  },
  "license": "UNLICENSED"
}
